# Backend Server Settings
API_PORT=8000
NODE_NAME=API_Principal_Nodo1

# Cassandra Settings
CASSANDRA_CONTACT_POINTS=127.0.0.1
CASSANDRA_PORT=9042
# CASSANDRA_USER=
# CASSANDRA_PASS=

# SMTP / Email Settings
# Choose mail service provider: "smtp" (default), "brevo" (HTTP API), or "sendgrid" (HTTP API)
# API-based email providers (Brevo, SendGrid) are recommended for hosting services like Render where SMTP ports are blocked.
MAIL_SERVICE=smtp

# Option 1: SMTP settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=manucat7891@gmail.com
SMTP_PASS=iodk tbym fiyf nhmn
SMTP_FROM_NAME=SEMAPA
SMTP_FROM_EMAIL=no-reply@semapa.gob.bo

# Option 2: Brevo HTTP API
BREVO_API_KEY=tu_api_key_de_brevo

# Option 3: SendGrid HTTP API
SENDGRID_API_KEY=tu_api_key_de_sendgrid
